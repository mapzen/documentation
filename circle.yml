machine:
  python:
    version: 3.4.3

dependencies:
  pre:
    - rm -rf node_modules # Force npm install to fetch styleguide from remote server

test:
  override:
    - make
    - ./run-checklist.py
